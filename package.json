{
  "name": "census-vector-tiles",
  "version": "0.0.1",
  "description": "census map example",
  "main": "index.html",
  "scripts": {
    "build": "rollup src/app.js --format iife --output dist/bundle.js --sourcemap",
    "watch": "rollup src/app.js --format iife --output dist/bundle.js --sourcemap --watch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "babel-version": "babel --version",
    "convert-datatree": "babel --plugins transform-es2015-modules-commonjs ./src/json/datatree.json --out-file ./breaks/datatree.js",
    "compute": "npm run convert-datatree && node breaks/compute-breaks.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/royhobbstn/censusVectorTiles.git"
  },
  "keywords": [
    "census",
    "vector",
    "tiles",
    "acs",
    "demography"
  ],
  "author": "Daniel Trone",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/royhobbstn/censusVectorTiles/issues"
  },
  "homepage": "https://github.com/royhobbstn/censusVectorTiles#readme",
  "devDependencies": {
    "babel-cli": "^6.24.0",
    "babel-plugin-transform-es2015-modules-commonjs": "^6.24.0",
    "rollup": "^0.41.5",
    "rollup-watch": "^3.2.2"
  },
  "dependencies": {
    "expr-eval": "^1.0.0",
    "source-map-support": "^0.4.14"
  }
}
